// loading component for suspense fallback
import * as React from 'react';

export default function Loader() {
    return (
        <div className="App">
            <h2>loading...</h2>
        </div>
    );
}
